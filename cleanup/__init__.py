"""Auto-cleanup: a safe, cross-platform disk cleanup assistant.

The package is split into a UI-agnostic ``core`` engine (scanning + safe
execution) and two thin front-ends (``cli`` and ``web``) that only present
the engine's results and collect the user's approval.
"""

__version__ = "0.1.0"
